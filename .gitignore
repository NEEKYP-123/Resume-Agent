# Secrets — never commit real credentials
backend/.env

# Local-mode data (tracking JSON + uploaded resume files — real PII)
backend/local_storage/

# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment (if ever created inside this folder)
venv/
.venv/

# OS
.DS_Store
Thumbs.db
